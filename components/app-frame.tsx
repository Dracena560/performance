'use client';
import { useState } from 'react';
import Link from 'next/link';
import { usePathname, useRouter } from 'next/navigation';
import { Activity, Clock3, Droplets, HeartPulse, LayoutDashboard, LogOut, Menu, Target, Trophy, Utensils, X, FileClock } from 'lucide-react';
import { logout } from '@/app/actions';
const nav=[['/hoje','Hoje',LayoutDashboard],['/agua','Água',Droplets],['/check-in','Check-in',Activity],['/alimentacao','Alimentação',Utensils],['/tenis','Tênis',Trophy],['/registros','Registros',Activity],['/perfil','Perfil',HeartPulse],['/historico','Histórico',FileClock],['/timeline','Timeline',Clock3],['/metas','Metas',Target]] as const;
export function AppFrame({children,title}:{children:React.ReactNode;title:string}){const pathname=usePathname(),router=useRouter(),[open,setOpen]=useState(false);return <div className="app-shell"><aside className={`sidebar ${open?'mobile-open':''}`}><Link href="/hoje" className="brand" onClick={()=>setOpen(false)}><HeartPulse size={28}/><span>felipe<span className="brand-dot">.</span><small>SAÚDE & PERFORMANCE</small></span></Link><button className="mobile-close" onClick={()=>setOpen(false)} aria-label="Fechar navegação"><X/></button><span className="nav-label">MEU DIÁRIO</span><nav>{nav.map(([href,label,Icon])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={pathname===href?'active':''} aria-current={pathname===href?'page':undefined}><Icon size={20}/>{label}{href==='/hoje'&&<span className="nav-today"/>}</Link>)}</nav><div className="sidebar-bottom"><div className="profile"><span className="avatar">F</span><span><strong>Felipe</strong><small>Espaço pessoal</small></span><button className="frame-logout" aria-label="Sair" onClick={()=>logout()}><LogOut size={18}/></button></div><small className="timezone">Europe/London</small></div></aside><main className="workspace"><header className="topbar"><button className="mobile-menu" onClick={()=>setOpen(value=>!value)} aria-label="Abrir navegação"><Menu/></button><span>Meu diário <span className="slash">/</span> <strong>{title}</strong></span><span className="topbar-note"><button className="refresh-button" onClick={()=>router.refresh()}>Atualizar</button><span className="tiny-dot"/>ESPAÇO PRIVADO</span></header>{children}</main></div>}
